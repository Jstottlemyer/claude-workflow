# Stub spec — clean-merged

Trivial change with full reviewer consensus. Used as the canonical happy-path fixture for autorun-policy integration smoke.

Acceptance: a single test passes; no security implications; no blocking findings.
