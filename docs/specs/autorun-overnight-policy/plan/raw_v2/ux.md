# UX v2 — Raw

**Recommendations:**
1. Startup banner in run.sh when `--mode` absent + non-interactive.
2. Spec table for 4 final_state × 2 recovery permutations.
3. `completed-no-pr` headline + body pinned (branch + last commit + suggested gh pr create).
4. Empty-stash + clean: "No backup needed (working tree was clean before reset)."
5. AC: `blocks[]` contains every `policy_block` call in order.
6. STAGE enum 11 needs human-readable label map.

**Open:** capture `gh` stderr into pr_creation_error? per-slug timeout? `pr` vs `pr-creation`?
